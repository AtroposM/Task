<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> <%= link_to "Edit a task", edit_task_path(task.id) %> <%= link_to "Delete", task_path(task.id), method: :delete%></li>
  <% end %>
</ul>

<%= link_to "Add a task", new_task_path %>
